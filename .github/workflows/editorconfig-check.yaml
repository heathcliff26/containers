---
name: "Editorconfig Check"

on:
  workflow_dispatch:
  push:
    branches: ["main", "renovate/*"]
  pull_request:
    branches: ["main"]
  merge_group:
    branches: ["main"]

jobs:
  check-editorconfig:
    uses: heathcliff26/ci/.github/workflows/editorconfig-check.yaml@2ca59152f717b61345534a04d61d49c0e5e69dbe # main
    permissions:
      contents: read
