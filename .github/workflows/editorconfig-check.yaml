---
name: "Editorconfig Check"

on:
  workflow_dispatch:
  push:
    branches: ["main", "renovate/*"]
  pull_request:
    branches: ["main"]
  merge_group:
    branches: ["main"]

jobs:
  check-editorconfig:
    uses: heathcliff26/ci/.github/workflows/editorconfig-check.yaml@42ac7a8dcdf67ef1e18a0082966072cce75a0fe4 # main
    permissions:
      contents: read
